/* ============================================================================
   CONFIGURA AQUÍ TU PROYECTO SUPABASE  (ver pasos en el README)
   Supabase Dashboard → Settings → API.
   Estos DOS valores son PÚBLICOS (van en el navegador, protegidos por las
   políticas RLS). NO pongas aquí la "service_role" key ni la contraseña de la BD.
   ============================================================================ */
window.SUPABASE_URL      = "https://TU-PROYECTO.supabase.co";
window.SUPABASE_ANON_KEY = "TU_ANON_KEY";

/* Correo del ADMINISTRADOR principal. Debe ser el MISMO que pongas en
   supabase-setup.sql (función handle_new_user). Ese correo recibe el rol admin. */
window.ADMIN_EMAIL = "admin@funcecaind.edu.ve";
