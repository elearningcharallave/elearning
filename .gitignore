# Dependencias / build (por si luego se agrega tooling)
node_modules/
dist/
.cache/

# Sistema
.DS_Store
Thumbs.db
*.log

# Editores
.vscode/
.idea/

# Supabase CLI (temporales)
supabase/.temp/
supabase/.branches/

# Secretos (NUNCA subir) — token de Supabase, llave JaaS y archivos locales
.supabase-token
.jaas-key.pem
*.pem
*.local
.env
.env.*

# Claves de JaaS/Jitsi exportadas por el navegador. El .pk es una clave
# PRIVADA de verdad; el .gitignore cubria *.pem pero no esta extension, y el
# archivo estaba en la carpeta sin trackear esperando a que un git add -A se
# lo llevara al repo.
*.pk
*.pub
